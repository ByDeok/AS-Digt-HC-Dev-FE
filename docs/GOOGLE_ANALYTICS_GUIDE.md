# Google Analytics 서비스 분석 가이드

> **AS-Digt-HC 시니어 웰니스 플랫폼을 위한 데이터 기반 의사결정 가이드**

이 문서는 Google Analytics 4 (GA4)를 활용하여 프로젝트의 사용자 행동을 분석하고, 서비스 개선을 위한 인사이트를 도출하는 방법을 안내합니다.

---

## 📋 목차

1. [시작하기 전에](#-시작하기-전에)
2. [프로젝트 특성 분석](#-프로젝트-특성-분석)
3. [핵심 성과 지표 (KPI)](#-핵심-성과-지표-kpi)
4. [이벤트 설계 전략](#-이벤트-설계-전략)
5. [페이지별 추적 포인트](#-페이지별-추적-포인트)
6. [백엔드 연동 추적](#-백엔드-연동-추적)
7. [사용자 세그먼트](#-사용자-세그먼트)
8. [퍼널 분석 설계](#-퍼널-분석-설계)
9. [대시보드 구성 예시](#-대시보드-구성-예시)
10. [구현 가이드](#-구현-가이드)

---

## 🎯 시작하기 전에

### Google Analytics란?

Google Analytics는 웹사이트와 앱의 사용자 행동을 추적하고 분석하는 무료 도구입니다.

```
사용자 행동 → 이벤트 수집 → 데이터 분석 → 인사이트 도출 → 서비스 개선
```

### 왜 GA4를 사용해야 하나요?

| 장점 | 설명 |
|------|------|
| **무료** | 기본 기능은 완전 무료 |
| **이벤트 기반** | 모든 사용자 행동을 이벤트로 추적 |
| **크로스 플랫폼** | 웹 + 앱 통합 분석 가능 |
| **AI 인사이트** | 자동화된 이상 징후 탐지 |
| **BigQuery 연동** | 원시 데이터 분석 가능 |

### 핵심 용어 정리

| 용어 | 설명 | 예시 |
|------|------|------|
| **Event (이벤트)** | 사용자가 수행한 행동 | 버튼 클릭, 페이지 조회 |
| **Parameter (매개변수)** | 이벤트의 추가 정보 | 클릭한 버튼 이름, 페이지 URL |
| **User Property (사용자 속성)** | 사용자의 특성 | 연령대, 회원 등급 |
| **Conversion (전환)** | 비즈니스 목표 달성 | 회원가입 완료, 리포트 생성 |
| **Session (세션)** | 사용자의 방문 단위 | 앱 실행 ~ 종료 |
| **Engagement (참여)** | 적극적인 상호작용 | 10초 이상 체류, 스크롤 |

---

## 📊 프로젝트 특성 분석

### 서비스 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                    AS-Digt-HC 서비스 구조                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   [랜딩] → [회원가입/로그인] → [온보딩] → [메인 서비스]           │
│                                              │                  │
│                                    ┌─────────┼─────────┐        │
│                                    ▼         ▼         ▼        │
│                               [대시보드] [리포트] [가족보드]     │
│                                    │                            │
│                              [미션 수행]                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 타겟 사용자 특성

| 구분 | Primary | Secondary |
|------|---------|-----------|
| **대상** | 시니어 (60세+) | 가족 구성원 (30-50대) |
| **기술 수준** | 낮음~중간 | 중간~높음 |
| **사용 패턴** | 짧은 세션, 반복 방문 | 비정기 확인 |
| **핵심 니즈** | 건강 미션 수행 | 가족 건강 모니터링 |
| **주요 기기** | 모바일 (태블릿) | 모바일 + 데스크톱 |

### 비즈니스 목표와 분석 목표 연결

| 비즈니스 목표 | 분석 목표 | 측정 지표 |
|--------------|----------|----------|
| 사용자 확보 | 획득 채널 효과 분석 | 채널별 신규 사용자 수 |
| 온보딩 완료율 향상 | 이탈 지점 파악 | 단계별 완료율 |
| 일일 활성 사용자 증가 | 재방문 패턴 분석 | DAU, 미션 완료율 |
| 가족 연결 활성화 | 초대/참여 전환율 | 초대 발송 → 수락 비율 |
| 건강 데이터 축적 | 기록 빈도 분석 | 건강 지표 입력 횟수 |

---

## 🎯 핵심 성과 지표 (KPI)

### Tier 1: 북극성 지표 (North Star Metrics)

> 서비스의 핵심 가치를 대표하는 단일 지표

| 지표 | 정의 | 목표 |
|------|------|------|
| **주간 미션 완료율** | 완료된 미션 / 할당된 미션 × 100 | 70% 이상 |

### Tier 2: 핵심 KPI

| 카테고리 | 지표 | 정의 | 측정 주기 |
|----------|------|------|----------|
| **획득** | 신규 가입자 수 | 회원가입 완료한 사용자 | 일별 |
| **활성화** | 온보딩 완료율 | 온보딩 완료 / 회원가입 × 100 | 주별 |
| **참여** | DAU (일일 활성 사용자) | 하루 1회 이상 방문한 사용자 | 일별 |
| **참여** | 미션 참여율 | 미션 시도 사용자 / DAU × 100 | 일별 |
| **리텐션** | D7 리텐션 | 가입 7일 후 재방문 비율 | 주별 |
| **확산** | 가족 초대 전환율 | 초대 수락 / 초대 발송 × 100 | 주별 |

### Tier 3: 기능별 지표

```
┌────────────────────────────────────────────────────────────────┐
│                      기능별 상세 지표                           │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  [온보딩]                 [대시보드]              [리포트]      │
│  ├─ 단계별 완료율         ├─ 미션 완료율           ├─ 생성 횟수  │
│  ├─ 평균 소요 시간        ├─ 미션 스킵율           ├─ 조회 횟수  │
│  └─ 이탈 단계 분포        └─ 건강기록 입력율       └─ 공유 횟수  │
│                                                                │
│  [가족보드]              [기기연동]                             │
│  ├─ 초대 발송 수          ├─ 연동 시도율                        │
│  ├─ 초대 수락율           ├─ 연동 성공율                        │
│  └─ 멤버당 활동 수        └─ 동기화 빈도                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 📝 이벤트 설계 전략

### 이벤트 명명 규칙

```
[동사]_[대상]_[세부사항(선택)]

예시:
- click_cta_landing        (랜딩 페이지 CTA 클릭)
- complete_onboarding      (온보딩 완료)
- submit_health_record     (건강 기록 제출)
- generate_report_weekly   (주간 리포트 생성)
```

### 이벤트 계층 구조

```
Level 1: 자동 수집 이벤트 (GA4 기본)
├── page_view          (페이지 조회)
├── session_start      (세션 시작)
├── first_visit        (첫 방문)
├── scroll             (스크롤 90%)
└── click              (외부 링크 클릭)

Level 2: 향상된 측정 이벤트 (GA4 설정)
├── file_download      (파일 다운로드)
├── video_start        (비디오 재생)
└── form_start         (폼 입력 시작)

Level 3: 커스텀 이벤트 (직접 구현) ⭐
├── 인증 관련
│   ├── signup_start
│   ├── signup_complete
│   ├── login_success
│   └── login_fail
├── 온보딩 관련
│   ├── onboarding_step_view
│   ├── onboarding_step_complete
│   └── onboarding_complete
├── 미션 관련
│   ├── mission_view
│   ├── mission_complete
│   └── mission_skip
├── 건강 기록 관련
│   ├── health_record_start
│   └── health_record_submit
├── 리포트 관련
│   ├── report_generate
│   ├── report_view
│   └── report_share
└── 가족 보드 관련
    ├── family_invite_send
    ├── family_invite_accept
    └── family_member_view
```

### 이벤트 매개변수 설계

#### 공통 매개변수

| 매개변수 | 타입 | 설명 | 예시 |
|----------|------|------|------|
| `user_id` | string | 로그인 사용자 ID | "usr_abc123" |
| `session_id` | string | 세션 ID (ULID) | "01HJ7X8Y..." |
| `timestamp` | number | Unix timestamp | 1706140800000 |
| `page_location` | string | 현재 페이지 URL | "/dashboard" |
| `user_type` | string | 사용자 유형 | "senior", "family" |

#### 이벤트별 매개변수

```typescript
// 온보딩 단계 완료
{
  event: "onboarding_step_complete",
  params: {
    step_name: "profile",           // 단계명
    step_number: 2,                 // 단계 번호 (1-4)
    step_duration_sec: 45,          // 소요 시간 (초)
    fields_completed: 4,            // 입력 완료 필드 수
    total_fields: 5                 // 전체 필드 수
  }
}

// 미션 완료
{
  event: "mission_complete",
  params: {
    mission_id: "act_001",          // 미션 ID
    mission_title: "30분 산책",      // 미션 제목
    mission_category: "exercise",   // 카테고리
    completion_time_sec: 1800,      // 완료까지 시간
    is_first_mission_today: true    // 오늘 첫 미션 여부
  }
}

// 건강 기록 제출
{
  event: "health_record_submit",
  params: {
    record_date: "2026-01-24",      // 기록 날짜
    metrics_count: 4,               // 입력한 지표 수
    has_blood_pressure: true,       // 혈압 입력 여부
    has_weight: true,               // 체중 입력 여부
    has_steps: true,                // 걸음 수 입력 여부
    has_heart_rate: false           // 심박수 입력 여부
  }
}

// 리포트 생성
{
  event: "report_generate",
  params: {
    report_type: "weekly",          // 리포트 유형
    period_start: "2026-01-18",     // 기간 시작
    period_end: "2026-01-24",       // 기간 종료
    data_completeness: 85           // 데이터 완성도 (%)
  }
}

// 가족 초대 발송
{
  event: "family_invite_send",
  params: {
    intended_role: "viewer",        // 초대 역할
    invite_method: "email",         // 초대 방법
    inviter_role: "admin"           // 초대자 역할
  }
}
```

---

## 📍 페이지별 추적 포인트

### 1. 랜딩 페이지 (`/`)

```
┌─────────────────────────────────────────────────────────────────┐
│                        랜딩 페이지                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [추적 포인트]                                                   │
│                                                                 │
│  1. 페이지 조회 (자동)                                           │
│     └─ page_view                                                │
│                                                                 │
│  2. 스크롤 깊이                                                  │
│     └─ scroll_depth: 25%, 50%, 75%, 90%                         │
│                                                                 │
│  3. CTA 버튼 클릭                                                │
│     ├─ click_cta: "무료로 시작하기"                              │
│     └─ click_cta: "자세히 알아보기"                              │
│                                                                 │
│  4. 섹션 노출 (Intersection Observer)                           │
│     ├─ view_section: "hero"                                     │
│     ├─ view_section: "features"                                 │
│     ├─ view_section: "testimonials"                             │
│     └─ view_section: "pricing"                                  │
│                                                                 │
│  5. 이탈 시점 (beforeunload)                                    │
│     └─ exit_intent: scroll_depth, time_on_page                  │
│                                                                 │
│  [인사이트 질문]                                                 │
│  - 어떤 섹션까지 스크롤했을 때 전환율이 높은가?                   │
│  - CTA 클릭 전 평균 체류 시간은?                                 │
│  - 어떤 트래픽 소스가 가장 높은 전환율을 보이는가?                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2. 인증 페이지 (`/login`, `/signup`)

| 추적 이벤트 | 트리거 시점 | 매개변수 | 인사이트 |
|------------|------------|----------|----------|
| `signup_start` | 회원가입 페이지 진입 | source_page | 유입 경로 |
| `signup_form_interact` | 첫 필드 입력 | field_name | 폼 참여율 |
| `signup_submit` | 제출 버튼 클릭 | - | 시도 횟수 |
| `signup_success` | 가입 성공 | time_to_complete | 전환율 |
| `signup_error` | 가입 실패 | error_type, error_field | 에러 분석 |
| `login_success` | 로그인 성공 | login_method | 방식 분석 |
| `login_fail` | 로그인 실패 | error_type | 실패 원인 |

### 3. 온보딩 플로우 (`/onboarding/*`)

```
┌─────────────────────────────────────────────────────────────────┐
│                      온보딩 퍼널 추적                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 1: 소셜 로그인 (/onboarding)                               │
│  ├─ onboarding_start                                            │
│  ├─ social_login_click: { provider: "kakao" | "naver" }        │
│  └─ 예상 완료율: 100% → 실제: ?%                                 │
│           │                                                     │
│           ▼                                                     │
│  Step 2: 프로필 입력 (/onboarding/profile)                       │
│  ├─ profile_step_start                                          │
│  ├─ profile_field_complete: { field: "name" | "birth" | ... }   │
│  ├─ profile_step_complete: { duration_sec, fields_filled }      │
│  └─ 예상 완료율: 90% → 실제: ?%                                  │
│           │                                                     │
│           ▼                                                     │
│  Step 3: 기기 연동 (/onboarding/device)                          │
│  ├─ device_step_start                                           │
│  ├─ device_connect_attempt: { device_type }                     │
│  ├─ device_connect_success / device_connect_fail                │
│  ├─ device_step_skip                                            │
│  └─ 예상 완료율: 80% → 실제: ?%                                  │
│           │                                                     │
│           ▼                                                     │
│  Step 4: 완료 (/onboarding/complete)                             │
│  ├─ onboarding_complete: { total_duration_sec, devices_count }  │
│  └─ 예상 완료율: 95% → 실제: ?%                                  │
│                                                                 │
│  [퍼널 분석 질문]                                                │
│  - 가장 많이 이탈하는 단계는?                                    │
│  - 프로필 입력에서 어떤 필드에서 이탈하는가?                     │
│  - 기기 연동을 건너뛰는 비율은?                                  │
│  - 온보딩 완료까지 평균 소요 시간은?                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4. 대시보드 (`/dashboard`)

| 추적 이벤트 | 트리거 시점 | 매개변수 | 인사이트 질문 |
|------------|------------|----------|--------------|
| `dashboard_view` | 페이지 진입 | missions_count, completed_count | 일일 미션 현황 |
| `mission_card_expand` | 미션 카드 펼침 | mission_id | 관심 미션 |
| `mission_complete` | 완료 버튼 클릭 | mission_id, duration | 완료 패턴 |
| `mission_skip` | 스킵 버튼 클릭 | mission_id, skip_reason | 스킵 이유 |
| `health_record_open` | 기록 모달 열기 | - | 기록 시도율 |
| `health_record_submit` | 기록 저장 | metrics_filled[] | 입력 지표 |
| `confetti_trigger` | 폭죽 효과 발생 | trigger_type | 동기부여 효과 |

### 5. 건강 리포트 (`/report`)

| 추적 이벤트 | 트리거 시점 | 매개변수 | 인사이트 질문 |
|------------|------------|----------|--------------|
| `report_page_view` | 페이지 진입 | reports_count | 리포트 보유 현황 |
| `report_type_toggle` | 주간/월간 토글 | report_type | 선호 기간 |
| `report_generate_click` | 생성 버튼 클릭 | report_type | 생성 시도 |
| `report_generate_success` | 생성 완료 | report_id, data_coverage | 생성 성공률 |
| `report_view` | 리포트 상세 조회 | report_id | 조회 패턴 |
| `report_share` | 공유 버튼 클릭 | share_method | 공유 방식 |
| `report_delete` | 삭제 버튼 클릭 | report_id | 삭제 이유 |

### 6. 가족 보드 (`/family`)

| 추적 이벤트 | 트리거 시점 | 매개변수 | 인사이트 질문 |
|------------|------------|----------|--------------|
| `family_board_view` | 페이지 진입 | member_count, user_role | 보드 구성 |
| `family_invite_open` | 초대 모달 열기 | - | 초대 의도 |
| `family_invite_send` | 초대 발송 | intended_role | 초대 역할 분포 |
| `family_invite_copy_code` | 코드 복사 | - | 코드 공유 |
| `family_join_attempt` | 코드 입력 참여 시도 | - | 참여 시도율 |
| `family_join_success` | 참여 성공 | invite_source | 참여 경로 |
| `family_member_role_change` | 역할 변경 | old_role, new_role | 역할 조정 |

---

## 🔗 백엔드 연동 추적

### 서버 사이드 이벤트

프론트엔드만으로 추적하기 어려운 데이터는 백엔드에서 Measurement Protocol을 통해 전송합니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                    백엔드 추적 이벤트                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [API 호출 성공/실패]                                            │
│  ├─ api_success: { endpoint, duration_ms, status_code }         │
│  └─ api_error: { endpoint, error_type, error_message }          │
│                                                                 │
│  [비동기 작업 완료]                                              │
│  ├─ report_generation_complete: { report_id, processing_time }  │
│  └─ device_sync_complete: { device_id, records_synced }         │
│                                                                 │
│  [보안/인증 이벤트]                                              │
│  ├─ token_refresh: { user_id }                                  │
│  ├─ session_expired: { user_id, last_activity }                 │
│  └─ suspicious_activity: { user_id, activity_type }             │
│                                                                 │
│  [비즈니스 크리티컬 이벤트]                                       │
│  ├─ subscription_change: { user_id, plan, action }              │
│  └─ data_export_request: { user_id, export_type }               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Request ID 기반 연결

프론트엔드와 백엔드 로그를 ULID 기반 `X-Request-ID`로 연결합니다.

```
[Frontend Event]                    [Backend Log]
     │                                   │
     │  X-Request-ID: 01HJ7X8Y...        │
     │ ──────────────────────────────────▶
     │                                   │
     │  requestId: 01HJ7X8Y...           │
     │                                   │
     ▼                                   ▼
┌─────────────┐                   ┌─────────────┐
│ GA4 Event   │                   │ API Log     │
│ session_id  │◄─────────────────▶│ requestId   │
│ user_id     │                   │ userId      │
└─────────────┘                   └─────────────┘
```

---

## 👥 사용자 세그먼트

### 세그먼트 정의

| 세그먼트 | 조건 | 분석 목적 |
|----------|------|----------|
| **신규 사용자** | first_visit 이후 7일 이내 | 온보딩 최적화 |
| **시니어 사용자** | user_type = "senior" | UX 개선 |
| **가족 사용자** | user_type = "family" | 참여 패턴 |
| **활성 사용자** | 주 3회+ 방문 | 충성도 분석 |
| **이탈 위험** | 7일+ 미방문 | 리텐션 캠페인 |
| **파워 유저** | 미션 완료율 80%+ | 성공 패턴 |
| **기기 연동 사용자** | has_device = true | 데이터 품질 |

### 코호트 분석

```
┌─────────────────────────────────────────────────────────────────┐
│                      코호트 리텐션 분석                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  가입 주차 │ Day 1 │ Day 7 │ Day 14 │ Day 30 │ Day 60 │        │
│  ─────────┼───────┼───────┼────────┼────────┼────────┤        │
│  Week 1   │ 100%  │  65%  │   52%  │   38%  │   28%  │        │
│  Week 2   │ 100%  │  70%  │   58%  │   42%  │        │        │
│  Week 3   │ 100%  │  68%  │   55%  │        │        │        │
│  Week 4   │ 100%  │  72%  │        │        │        │        │
│                                                                 │
│  [분석 질문]                                                     │
│  - Day 7 리텐션이 70% 미만인 코호트의 특징은?                    │
│  - 온보딩 완료 여부에 따른 리텐션 차이는?                        │
│  - 기기 연동 사용자의 리텐션이 더 높은가?                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 퍼널 분석 설계

### 핵심 퍼널

#### 1. 신규 사용자 전환 퍼널

```
랜딩 방문 → CTA 클릭 → 회원가입 시작 → 가입 완료 → 온보딩 완료 → 첫 미션 완료
    100%       45%          38%           30%           25%           18%

[최적화 포인트]
- 랜딩 → CTA: 가치 제안 강화
- 가입 시작 → 완료: 폼 간소화
- 온보딩 → 미션: 첫 경험 개선
```

#### 2. 일일 사용자 참여 퍼널

```
앱 열기 → 미션 확인 → 미션 시작 → 미션 완료
  100%       85%         60%         52%

[최적화 포인트]
- 미션 확인 → 시작: 미션 난이도 조정
- 시작 → 완료: 완료 인센티브 강화
```

#### 3. 리포트 생성 퍼널

```
리포트 페이지 방문 → 생성 버튼 클릭 → 생성 완료 → 리포트 조회 → 공유
       100%              65%             60%          55%         12%

[최적화 포인트]
- 조회 → 공유: 공유 기능 접근성 개선
```

#### 4. 가족 초대 퍼널

```
가족 보드 방문 → 초대 모달 열기 → 초대 발송 → 상대방 수락
     100%            35%            28%          15%

[최적화 포인트]
- 모달 열기 → 발송: 초대 프로세스 간소화
- 발송 → 수락: 초대 메시지 개선
```

---

## 📈 대시보드 구성 예시

### 경영진용 대시보드

```
┌─────────────────────────────────────────────────────────────────┐
│                     Executive Dashboard                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ 신규 가입   │  │ DAU         │  │ 미션 완료율 │             │
│  │   +245      │  │   1,234     │  │    68%      │             │
│  │   ▲ 12%     │  │   ▲ 8%      │  │    ▲ 5%     │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  ┌─────────────────────────────────────────────────┐           │
│  │          주간 활성 사용자 트렌드                  │           │
│  │  ^                                              │           │
│  │  │    ╭──╮     ╭──╮                            │           │
│  │  │   ╭╯  ╰─╮ ╭─╯  ╰─╮                          │           │
│  │  │  ╭╯     ╰─╯      ╰─                         │           │
│  │  └──────────────────────────────────────▶      │           │
│  │     Mon  Tue  Wed  Thu  Fri  Sat  Sun          │           │
│  └─────────────────────────────────────────────────┘           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 제품팀용 대시보드

```
┌─────────────────────────────────────────────────────────────────┐
│                     Product Dashboard                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [온보딩 퍼널]              [기능별 사용량]                       │
│  Step 1: ████████████ 100%   대시보드:  ████████████ 89%        │
│  Step 2: █████████░░░  85%   리포트:    ██████░░░░░░ 52%        │
│  Step 3: ███████░░░░░  68%   가족보드:  ████░░░░░░░░ 34%        │
│  Step 4: ██████░░░░░░  62%                                      │
│                                                                 │
│  [이탈 지점 분석]           [에러 발생 Top 5]                    │
│  프로필 입력: 15%            1. 네트워크 타임아웃 (12%)          │
│  기기 연동: 17%              2. 세션 만료 (8%)                   │
│  미션 스킵: 22%              3. 폼 유효성 실패 (5%)              │
│                              4. 파일 업로드 실패 (3%)            │
│                              5. API 500 에러 (2%)                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🛠 구현 가이드

### Step 1: GA4 설정

1. **GA4 속성 생성**
   - [Google Analytics](https://analytics.google.com) 접속
   - 관리 > 속성 만들기
   - 웹 스트림 추가

2. **Measurement ID 획득**
   ```
   G-XXXXXXXXXX
   ```

### Step 2: 프론트엔드 설치

#### React + gtag.js 방식

```bash
# 패키지 설치 불필요 (gtag.js는 스크립트로 로드)
```

```typescript
// src/lib/analytics.ts

/**
 * Google Analytics 4 통합 모듈
 * 
 * 사용자 행동 추적 및 이벤트 전송을 담당합니다.
 * - ULID 기반 세션 ID로 프론트엔드-백엔드 로그 연결
 * - 환경변수로 추적 활성화/비활성화
 */

// GA4 측정 ID (환경변수에서 로드)
const GA_MEASUREMENT_ID = import.meta.env.VITE_GA_MEASUREMENT_ID;

// 개발 환경에서 추적 비활성화 옵션
const IS_TRACKING_ENABLED = 
  import.meta.env.PROD || 
  import.meta.env.VITE_ENABLE_ANALYTICS === 'true';

/**
 * gtag 함수 타입 정의
 */
declare global {
  interface Window {
    gtag: (...args: unknown[]) => void;
    dataLayer: unknown[];
  }
}

/**
 * GA4 초기화
 * index.html 또는 App.tsx에서 호출
 */
export function initGA(): void {
  if (!IS_TRACKING_ENABLED || !GA_MEASUREMENT_ID) {
    console.log('[Analytics] Tracking disabled');
    return;
  }

  // gtag.js 스크립트 로드
  const script = document.createElement('script');
  script.async = true;
  script.src = `https://www.googletagmanager.com/gtag/js?id=${GA_MEASUREMENT_ID}`;
  document.head.appendChild(script);

  // dataLayer 초기화
  window.dataLayer = window.dataLayer || [];
  window.gtag = function gtag(...args: unknown[]) {
    window.dataLayer.push(args);
  };

  window.gtag('js', new Date());
  window.gtag('config', GA_MEASUREMENT_ID, {
    send_page_view: false, // 수동으로 페이지뷰 전송
    debug_mode: import.meta.env.DEV,
  });

  console.log('[Analytics] GA4 initialized');
}

/**
 * 페이지뷰 이벤트 전송
 */
export function trackPageView(pagePath: string, pageTitle?: string): void {
  if (!IS_TRACKING_ENABLED) return;

  window.gtag?.('event', 'page_view', {
    page_path: pagePath,
    page_title: pageTitle || document.title,
  });
}

/**
 * 커스텀 이벤트 전송
 */
export function trackEvent(
  eventName: string,
  params?: Record<string, unknown>
): void {
  if (!IS_TRACKING_ENABLED) return;

  window.gtag?.('event', eventName, {
    ...params,
    timestamp: Date.now(),
  });

  // 개발 환경에서 콘솔 로그
  if (import.meta.env.DEV) {
    console.log('[Analytics Event]', eventName, params);
  }
}

/**
 * 사용자 속성 설정
 */
export function setUserProperties(properties: Record<string, unknown>): void {
  if (!IS_TRACKING_ENABLED) return;

  window.gtag?.('set', 'user_properties', properties);
}

/**
 * 사용자 ID 설정 (로그인 시)
 */
export function setUserId(userId: string | null): void {
  if (!IS_TRACKING_ENABLED) return;

  window.gtag?.('config', GA_MEASUREMENT_ID, {
    user_id: userId,
  });
}

// ============================================
// 사전 정의된 이벤트 헬퍼 함수
// ============================================

/** 회원가입 시작 */
export const trackSignupStart = (sourcePage: string) =>
  trackEvent('signup_start', { source_page: sourcePage });

/** 회원가입 완료 */
export const trackSignupComplete = (timeToComplete: number) =>
  trackEvent('signup_complete', { time_to_complete_sec: timeToComplete });

/** 로그인 성공 */
export const trackLoginSuccess = (method: string) =>
  trackEvent('login_success', { login_method: method });

/** 온보딩 단계 완료 */
export const trackOnboardingStep = (
  stepName: string,
  stepNumber: number,
  durationSec: number
) =>
  trackEvent('onboarding_step_complete', {
    step_name: stepName,
    step_number: stepNumber,
    step_duration_sec: durationSec,
  });

/** 온보딩 완료 */
export const trackOnboardingComplete = (totalDurationSec: number) =>
  trackEvent('onboarding_complete', {
    total_duration_sec: totalDurationSec,
  });

/** 미션 완료 */
export const trackMissionComplete = (
  missionId: string,
  missionTitle: string,
  category: string
) =>
  trackEvent('mission_complete', {
    mission_id: missionId,
    mission_title: missionTitle,
    mission_category: category,
  });

/** 미션 스킵 */
export const trackMissionSkip = (missionId: string, reason?: string) =>
  trackEvent('mission_skip', {
    mission_id: missionId,
    skip_reason: reason,
  });

/** 건강 기록 제출 */
export const trackHealthRecordSubmit = (metrics: string[]) =>
  trackEvent('health_record_submit', {
    metrics_count: metrics.length,
    metrics_types: metrics,
  });

/** 리포트 생성 */
export const trackReportGenerate = (reportType: 'weekly' | 'monthly') =>
  trackEvent('report_generate', { report_type: reportType });

/** 가족 초대 발송 */
export const trackFamilyInviteSend = (intendedRole: string) =>
  trackEvent('family_invite_send', { intended_role: intendedRole });

/** 가족 초대 수락 */
export const trackFamilyJoinSuccess = () =>
  trackEvent('family_join_success', {});

/** CTA 클릭 */
export const trackCtaClick = (ctaName: string, ctaLocation: string) =>
  trackEvent('click_cta', {
    cta_name: ctaName,
    cta_location: ctaLocation,
  });

/** 에러 발생 */
export const trackError = (
  errorType: string,
  errorMessage: string,
  errorLocation: string
) =>
  trackEvent('error_occurred', {
    error_type: errorType,
    error_message: errorMessage,
    error_location: errorLocation,
  });
```

### Step 3: 라우터 통합

```typescript
// src/App.tsx 또는 라우터 설정 파일

import { useEffect } from 'react';
import { useLocation } from 'react-router-dom';
import { initGA, trackPageView } from '@/lib/analytics';

function App() {
  const location = useLocation();

  // GA4 초기화 (앱 마운트 시 1회)
  useEffect(() => {
    initGA();
  }, []);

  // 페이지 변경 시 페이지뷰 추적
  useEffect(() => {
    trackPageView(location.pathname);
  }, [location.pathname]);

  return (
    // ... 라우터 구성
  );
}
```

### Step 4: 컴포넌트에서 이벤트 추적

```typescript
// 예시: 대시보드에서 미션 완료 추적

import { trackMissionComplete, trackMissionSkip } from '@/lib/analytics';

function MissionCard({ mission }: { mission: Mission }) {
  const handleComplete = async () => {
    await completeMission(mission.id);
    
    // GA4 이벤트 추적
    trackMissionComplete(
      mission.id,
      mission.title,
      mission.category
    );
  };

  const handleSkip = async (reason?: string) => {
    await skipMission(mission.id);
    
    // GA4 이벤트 추적
    trackMissionSkip(mission.id, reason);
  };

  return (
    // ... 컴포넌트 JSX
  );
}
```

### Step 5: 환경변수 설정

```properties
# studio/.env.local

# Google Analytics 4 측정 ID
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# 개발 환경에서도 추적 활성화 (테스트용)
VITE_ENABLE_ANALYTICS=false
```

### Step 6: 디버깅

1. **GA4 DebugView 사용**
   - GA4 관리 > DebugView
   - 실시간 이벤트 확인

2. **Chrome 확장 프로그램**
   - [Google Analytics Debugger](https://chrome.google.com/webstore/detail/google-analytics-debugger)
   - 콘솔에서 gtag 호출 확인

3. **개발 환경 로그**
   ```typescript
   // analytics.ts에서 DEV 환경 콘솔 로그 확인
   if (import.meta.env.DEV) {
     console.log('[Analytics Event]', eventName, params);
   }
   ```

---

## 📚 참고 자료

### 공식 문서

| 자료 | URL |
|------|-----|
| GA4 개발자 가이드 | https://developers.google.com/analytics/devguides/collection/ga4 |
| gtag.js 레퍼런스 | https://developers.google.com/tag-platform/gtagjs/reference |
| 이벤트 레퍼런스 | https://developers.google.com/analytics/devguides/collection/ga4/reference/events |
| Measurement Protocol | https://developers.google.com/analytics/devguides/collection/protocol/ga4 |

### 추가 학습 자료

- [GA4 Academy](https://skillshop.exceedlms.com/student/catalog/list?category_ids=6431-google-analytics-4)
- [GA4 Best Practices](https://support.google.com/analytics/answer/9267735)

---

## 📝 체크리스트

### 구현 전 체크리스트

- [ ] GA4 속성 생성 완료
- [ ] Measurement ID 환경변수 설정
- [ ] 개인정보 처리방침에 분석 도구 사용 고지
- [ ] 쿠키 동의 배너 구현 (필요시)

### 구현 체크리스트

- [ ] analytics.ts 모듈 생성
- [ ] GA4 초기화 코드 추가
- [ ] 페이지뷰 자동 추적 설정
- [ ] 핵심 이벤트 추적 구현
  - [ ] 회원가입/로그인
  - [ ] 온보딩 단계
  - [ ] 미션 완료/스킵
  - [ ] 건강 기록 제출
  - [ ] 리포트 생성
  - [ ] 가족 초대/참여
- [ ] 사용자 속성 설정
- [ ] 에러 추적 구현

### 검증 체크리스트

- [ ] DebugView에서 이벤트 수신 확인
- [ ] 실시간 보고서에서 데이터 확인
- [ ] 퍼널 보고서 생성 테스트
- [ ] 세그먼트 필터링 테스트

---

## 📄 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0 | 2026-01-24 | 최초 작성 |

---

## 🤝 기여 및 문의

이 가이드에 대한 질문이나 개선 제안은 프로젝트 이슈를 통해 등록해주세요.
