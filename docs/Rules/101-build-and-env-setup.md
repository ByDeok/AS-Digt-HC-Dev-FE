# 빌드 프로세스
- 선택한 언어/프레임워크에 따라 다름

# 개발용 환경 변수
- 필요한 개발 변수가 포함된 `.env` 파일 추가

# 배포용 환경 변수
- 배포에 필요한 `.env` 또는 설정 파일 명시

# 현재 파일 구조
tree -L 4 -a -I 'node_modules|.git|__pycache__|.DS_Store|.pytest_cache|.vscode'
